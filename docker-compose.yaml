version: '3.7'

services:
  web_marcaciones:
    image: nginx:stable-alpine3.19-slim
    container_name: web_marcaciones
    volumes:
      - ./client/dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    networks:
      - red-gane-int

  api_marcaciones:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: api_marc_container
    image: api_marc:v1.0
    networks:
      - red-gane-int
      
networks:
  red-gane-int:
    external: true
